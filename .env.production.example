# Production Environment Variables
# Copy this to .env on production server and update values

# Database Configuration
# IMPORTANT: Change DB_HOST to your PostgreSQL container name
# This should match the container name in your database docker-compose
DB_HOST=postgres
# OR if your database container has a different name:
# DB_HOST=finance-db
# DB_HOST=james-postgres

DB_USER=user_finance
DB_PASSWORD=A9DtRovze2dtjdUMZcgYRjNymdEY9qSH
DB_NAME=finance
DB_PORT=5432

# OTE Portal Certificate Configuration (for authenticated downloads)
# Path to client certificate inside container (mounted via docker-compose)
OTE_CERT_PATH=/app/certs/ote_client.p12
# Certificate password
OTE_CERT_PASSWORD=your_cert_password_here
# Local storage password for portal (used during certificate import)
OTE_LOCAL_STORAGE_PASSWORD=MWy-iH3/u7-Fvc_C\sV9
